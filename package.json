{
  "scripts": {
    "build:pages": "sass --load-path=node_modules --no-source-map docs/styles.scss docs/styles.css"
  },
  "devDependencies": {
    "clamp-size": "^1.0.4",
    "sass": "^1.104.0"
  }
}
